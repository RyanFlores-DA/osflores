-- Table: public.dim_mes

-- DROP TABLE IF EXISTS public.dim_mes;

CREATE TABLE IF NOT EXISTS public.dim_mes
(
    mes_id integer NOT NULL DEFAULT nextval('dim_mes_mes_id_seq'::regclass),
    mes character varying(15) COLLATE pg_catalog."default",
    CONSTRAINT dim_mes_pkey PRIMARY KEY (mes_id)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.dim_mes
    OWNER to ryan;